//Tipos de dados primitivos

const name = "Thomas"; //String
const name2 = "Thomas";
const num = 30; //Number
const num2 = 30.2; //number

let nameAluno; //Undefined  -> Não aponta pra local nenhuma na memória
let sobreNomeAluno = null; //Nulo -> Não aponta pra local nenhuma na memória
const aprovado = false; //Boolean = true, false(lógico)

console.log(typeof sobreNomeAluno, aprovado);
